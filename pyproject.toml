[project]
name = "poetic-machines"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "py5canvas",
]

[tool.uv.sources]
py5canvas = { path = "../../../comparts/py5canvas" }
